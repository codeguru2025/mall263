# DO App Platform buildpacks set NODE_ENV=production during npm install,
# which skips devDependencies and breaks `nest build` (@nestjs/cli, typescript, prisma).
# This forces npm to always install all dependencies including devDependencies.
production=false
